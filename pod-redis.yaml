apiVersion: v1
kind: Pod
metadata:
  name: redis
  labels:
    app: redis
spec:
  restartPolicy: Always
  containers:
    
    - name: redis
      image: docker.io/library/redis:7-alpine
      ports:
        - containerPort: 6379
          hostPort: 6379
      args:
        - "redis-server"
        - "--databases"
        - "16"
        - "--maxmemory"
        - "512mb"
        - "--maxmemory-policy"
        - "allkeys-lru"
        - "--appendonly"
        - "yes"
        - "--appendfsync"
        - "everysec"
      volumeMounts:
        - mountPath: /data
          name: redis-data
      resources:
        requests:
          memory: "128Mi"
          cpu: "100m"
        limits:
          memory: "640Mi"
          cpu: "500m"

          
    - name: redisinsight
      image: docker.io/redis/redisinsight:latest
      ports:
        - containerPort: 5540
          hostPort: 6380
      env:
        - name: RI_APP_PORT
          value: "5540"
      envFrom:
        - secretRef:
            name: app-secrets
      resources:
        requests:
          memory: "128Mi"
          cpu: "100m"
        limits:
          memory: "512Mi"
          cpu: "500m"

    
    - name: redis-exporter
      image: oliver006/redis_exporter:v1.67.0
      ports:
        - containerPort: 9121
          hostPort: 9121
      env:
        - name: REDIS_ADDR
          value: "localhost:6379"
      envFrom:
        - secretRef:
            name: app-secrets
      resources:
        requests:
          memory: "32Mi"
          cpu: "50m"
        limits:
          memory: "128Mi"
          cpu: "200m"

  volumes:
    - name: redis-data
      hostPath:
        path: ${PERSISTENT_PATH}/redis
        type: DirectoryOrCreate